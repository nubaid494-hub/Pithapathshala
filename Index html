<!doctype html>
<html>
    <head> 
        <meta charset="UTF-8"> 
        <meta name="viewport" id="viewport" content="width=device-width, initial-scale=1"> 
        <link rel="shortcut icon" href="favicon.png" type="image/x-icon">
        <title>My Website</title>
    </head> 
    <body> 
        <h1>My Website</h1>
        <div>
            Hello World! 
        </div>  
    </body>
</html><!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pitha Pathshala | Command Center</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/all.min.css">
    
    <style>
        :root { --admin-bg: #f0f2f5; --sidebar-color: #1a1a1a; --primary: #d35400; }
        body { background: var(--admin-bg); font-family: 'Poppins', sans-serif; overflow-x: hidden; }

        /* Sidebar Navigation */
        .sidebar { height: 100vh; background: var(--sidebar-color); color: white; position: fixed; width: 260px; transition: 0.3s; z-index: 1000; }
        .sidebar .nav-link { color: #a2a2a2; padding: 15px 25px; border-left: 4px solid transparent; transition: 0.3s; }
        .sidebar .nav-link:hover, .sidebar .nav-link.active { color: white; background: #252525; border-left: 4px solid var(--primary); }
        
        .main-view { margin-left: 260px; padding: 40px; }
        
        /* Stats & Cards */
        .stat-card { border: none; border-radius: 15px; box-shadow: 0 5px 15px rgba(0,0,0,0.05); transition: 0.3s; }
        .stat-card:hover { transform: translateY(-5px); }
        
        .product-list-img { width: 45px; height: 45px; border-radius: 8px; background: #eee; display: flex; align-items: center; justify-content: center; }
        
        @media (max-width: 768px) {
            .sidebar { width: 0; overflow: hidden; }
            .main-view { margin-left: 0; }
        }
    </style>
</head>
<body>

    <div class="sidebar">
        <div class="p-4 mb-4">
            <h4 class="fw-bold text-white">Pitha <span style="color:var(--primary)">Pathshala</span></h4>
            <small class="text-muted">Master Admin v2.0</small>
        </div>
        <nav class="nav flex-column">
            <a class="nav-link active" href="#"><i class="fa fa-home me-2"></i> Dashboard</a>
            <a class="nav-link" href="#"><i class="fa fa-shopping-bag me-2"></i> Products</a>
            <a class="nav-link" href="#"><i class="fa fa-tags me-2"></i> Discounts</a>
            <a class="nav-link" href="#"><i class="fa fa-users me-2"></i> Customers</a>
            <a class="nav-link mt-5 text-warning" href="index.html"><i class="fa fa-eye me-2"></i> View Live Site</a>
        </nav>
    </div>

    <div class="main-view">
        <header class="d-flex justify-content-between align-items-center mb-5">
            <div>
                <h2 class="fw-bold">Store Management</h2>
                <p class="text-muted">Control your inventory and active promos.</p>
            </div>
            <button class="btn btn-dark rounded-pill px-4 py-2" data-bs-toggle="modal" data-bs-target="#addPithaModal">
                <i class="fa fa-plus me-2"></i> Add New Product
            </button>
        </header>

        <div class="row g-4 mb-5">
            <div class="col-md-3">
                <div class="stat-card p-4 bg-white">
                    <small class="text-muted text-uppercase fw-bold">Active Products</small>
                    <h2 class="fw-bold mt-2" id="count-products">0</h2>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stat-card p-4 bg-white border-bottom border-4 border-warning">
                    <small class="text-muted text-uppercase fw-bold">Active Promo</small>
                    <h2 class="fw-bold mt-2" id="current-promo">PITHA10</h2>
                </div>
            </div>
        </div>

        <div class="card stat-card p-4 bg-white">
            <h5 class="fw-bold mb-4">Inventory List</h5>
            <div class="table-responsive">
                <table class="table align-middle">
                    <thead>
                        <tr class="text-muted">
                            <th>Product</th>
                            <th>Category</th>
                            <th>Price</th>
                            <th>Discount</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody id="adminProductList">
                        </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="modal fade" id="addPithaModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-0 rounded-4">
                <div class="modal-header border-0 p-4">
                    <h5 class="fw-bold">New Pitha Details</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body p-4">
                    <div class="mb-3">
                        <label class="form-label fw-bold">Pitha Name</label>
                        <input type="text" id="pName" class="form-control" placeholder="e.g. Premium Nokshi">
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label fw-bold">Base Price (৳)</label>
                            <input type="number" id="pPrice" class="form-control">
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label fw-bold">Sale Price (৳)</label>
                            <input type="number" id="pSale" class="form-control">
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Category</label>
                        <select id="pCat" class="form-select">
                            <option>Winter Special</option>
                            <option>Frozen Snack</option>
                            <option>Sweet Box</option>
                        </select>
                    </div>
                </div>
                <div class="modal-footer border-0 p-4">
                    <button class="btn btn-warning w-100 fw-bold rounded-pill" onclick="pushToFirebase()">Publish to Store</button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-database-compat.js"></script>

    <script>
        // CONFIG: Replace with your Firebase Keys
        const firebaseConfig = {
            apiKey: "YOUR_API_KEY",
            databaseURL: "YOUR_DATABASE_URL",
            projectId: "YOUR_PROJECT_ID"
        };
        firebase.initializeApp(firebaseConfig);
        const db = firebase.database();

        // 1. PUSH TO FIREBASE
        function pushToFirebase() {
            const name = document.getElementById('pName').value;
            const price = document.getElementById('pPrice').value;
            const sale = document.getElementById('pSale').value;
            const cat = document.getElementById('pCat').value;

            if(name && price) {
                const newProductRef = db.ref('products').push();
                newProductRef.set({
                    name, price, sale, cat,
                    status: 'Available',
                    timestamp: Date.now()
                }).then(() => {
                    bootstrap.Modal.getInstance(document.getElementById('addPithaModal')).hide();
                    location.reload(); 
                });
            }
        }

        // 2. FETCH & RENDER
        db.ref('products').on('value', (snapshot) => {
            const list = document.getElementById('adminProductList');
            list.innerHTML = '';
            let count = 0;
            
            snapshot.forEach((child) => {
                const p = child.val();
                count++;
                list.innerHTML += `
                    <tr>
                        <td>
                            <div class="d-flex align-items-center">
                                <div class="product-list-img me-3">🥟</div>
                                <span class="fw-bold">${p.name}</span>
                            </div>
                        </td>
                        <td><span class="badge bg-light text-dark">${p.cat}</span></td>
                        <td>৳${p.price}</td>
                        <td class="text-success fw-bold">৳${p.sale || 'No'}</td>
                        <td><span class="badge bg-success">Active</span></td>
                        <td>
                            <button class="btn btn-sm btn-light" onclick="deleteItem('${child.key}')"><i class="fa fa-trash text-danger"></i></button>
                        </td>
                    </tr>
                `;
            });
            document.getElementById('count-products').innerText = count;
        });

        function deleteItem(key) {
            if(confirm("Remove this pitha from the live store?")) {
                db.ref('products/' + key).remove();
            }
        }
    </script>
</body>
</html
